name: model_config
description: AI model configuration and provider settings
version: 1.0.0

dependencies:
  # Valdi core dependencies
  valdi_core: "^1.0.0"

  # Application modules
  common: "1.0.0"
