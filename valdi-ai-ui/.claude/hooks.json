{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.(ts|tsx)$ ]]; then echo '[Type-check] Type-checking edited file...' && npx tsc --noEmit --skipLibCheck \"$CLAUDE_FILE_PATHS\" 2>&1 | head -20 || echo '[Warning] TypeScript errors detected in edited file'; fi",
            "description": "Type-check TypeScript files after edits"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.(ts|tsx)$ ]]; then echo '[Linting] Linting edited file...' && npx eslint --quiet \"$CLAUDE_FILE_PATHS\" 2>&1 | head -10 || echo '[Warning] ESLint warnings in edited file'; fi",
            "description": "Lint TypeScript files after edits"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.json$ ]]; then echo '[Validation] Validating JSON...' && jq empty \"$CLAUDE_FILE_PATHS\" 2>&1 || echo '[Error] Invalid JSON syntax'; fi",
            "description": "Validate JSON files after edits"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.(ts|tsx)$ ]]; then echo '[Type-check] Type-checking new file...' && npx tsc --noEmit --skipLibCheck \"$CLAUDE_FILE_PATHS\" 2>&1 | head -20 || echo '[Warning] TypeScript errors in new file'; fi",
            "description": "Type-check new TypeScript files"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.(ts|tsx)$ ]]; then echo '[Linting] Linting new file...' && npx eslint --quiet \"$CLAUDE_FILE_PATHS\" 2>&1 | head -10 || echo '[Warning] ESLint warnings in new file'; fi",
            "description": "Lint new TypeScript files"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.json$ ]]; then echo '[Validation] Validating JSON...' && jq empty \"$CLAUDE_FILE_PATHS\" 2>&1 || echo '[Error] Invalid JSON syntax'; fi",
            "description": "Validate new JSON files"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ package\\.json$ ]]; then echo '[Validation] Validating package.json structure...' && npm pkg fix 2>&1 || echo '[Warning] package.json validation warnings'; fi",
            "description": "Validate package.json structure"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_TOOL_PARAMS\" =~ (rm|rmdir|mv.*\\/dev\\/null|>.*\\/dev\\/null) ]]; then echo '[Warning] WARNING: Potentially destructive command detected!'; fi",
            "description": "Warn about destructive commands"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.(ts|tsx)$ ]]; then echo '[Doc-check] Checking JSDoc completeness...' && grep -q '/\\*\\*' \"$CLAUDE_FILE_PATHS\" && echo '[Pass] JSDoc comments found' || echo '[Info] Consider adding JSDoc comments for public APIs'; fi",
            "description": "Check for JSDoc documentation"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ package\\.json$ ]]; then echo '[Security] Running dependency audit...' && npm audit --audit-level=moderate 2>&1 | head -15 || echo '[Info] Dependency audit check complete'; fi",
            "description": "Security audit on package.json changes"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ BUILD\\.bazel$ ]]; then echo '[Build] Validating Bazel syntax...' && bazel query \"deps($CLAUDE_FILE_PATHS)\" --output=label 2>&1 | head -10 || echo '[Warning] Bazel validation failed - check syntax'; fi",
            "description": "Validate Bazel BUILD files"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATHS\" =~ \\.(ts|tsx)$ ]] && [[ -f \"$CLAUDE_FILE_PATHS\" ]]; then echo '[Backup] Creating backup: ${CLAUDE_FILE_PATHS}.backup' && cp \"$CLAUDE_FILE_PATHS\" \"${CLAUDE_FILE_PATHS}.backup\" 2>/dev/null || true; fi",
            "description": "Backup TypeScript files before editing"
          }
        ]
      }
    ]
  }
}
