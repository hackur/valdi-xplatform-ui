# Run Tests

Run the Jest test suite for the Valdi AI UI project.

```bash
npm test
```

For watch mode during development:

```bash
npm run test:watch
```

For coverage reporting:

```bash
npm run test:coverage
```

To run tests in CI mode:

```bash
npm run test:ci
```
